# Variabili d'ambiente — Diamanti Automobili
# Copia in `.env.local` (NON committato) per lo sviluppo, oppure configurale come
# Environment Variables su Vercel. NON mettere mai valori reali in questo file.

# --- Fase 7b (Resend) — NON ANCORA IN USO ---
# Endpoint lead proprio (api/lead.ts) via Resend. Da attivare quando ci saranno
# dominio email verificato + chiave. Finché è assente, l'invio resta su FormSubmit.
# Va usata SOLO server-side (Vercel Function), mai esposta nel client.
RESEND_API_KEY=

# Nota: l'ID GA4 (G-RYV2J7P6D5) è un identificativo PUBBLICO lato client
# (in public/analytics.js) e NON è un segreto: non va in env.
